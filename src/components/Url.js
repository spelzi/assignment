import React from "react";
import { products } from "../Product";

// import products img url to export to card div 
const Url = () => {
    return (
        <img src={products.url} />
    )
}

export default Url;